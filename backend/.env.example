# SQLite (default, dev) -> file lokal
DATABASE_URL="file:./dev.db"

# Kalau pindah ke PostgreSQL, contoh:
# DATABASE_URL="postgresql://user:password@localhost:5432/hse_fatigue?schema=public"

JWT_SECRET="ganti-dengan-secret-key-yang-kuat"
JWT_EXPIRES_IN="8h"
PORT=4000
CORS_ORIGIN="http://localhost:5173"
